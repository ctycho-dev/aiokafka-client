[project]
name = "aiokafka-client"
version = "0.1.0"
description = "Reusable aiokafka consumer/producer client with SSL/SASL support"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.13.0",
    "pydantic-settings>=2.13.1",
    "python-json-logger>=4.0.0",
]
